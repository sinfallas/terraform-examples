services:
  terraform:
    image: sinfallas/terraform-all-in-one:latest
    hostname: terraform
    container_name: terraform
    pull_policy: always
    restart: always
    ports:
      - 3000:3000
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION}
      - TF_VAR_aws_region=${AWS_DEFAULT_REGION}
      - TF_VAR_npm_api_url=${TF_VAR_npm_api_url}
      - TF_VAR_npm_token=${TF_VAR_npm_token}
      - TF_VAR_dominio=${TF_VAR_dominio}
      - TF_VAR_host_destino=${TF_VAR_host_destino}
      - TF_VAR_puerto_destino=${TF_VAR_puerto_destino}
      - TF_VAR_esquema_destino=${TF_VAR_esquema_destino}
      - TF_VAR_certificate_id=${TF_VAR_certificate_id}
    volumes:
#      - ./config.json:/etc/semaphore/config.json
#      - ./semaphore_data:/app
      - ./recursos:/recursos
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "1"
